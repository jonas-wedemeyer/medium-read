import React, { useState } from 'react';
import Moment from 'react-moment';

export default function CountDown({ date_added }) {
  const [timer, setTimer] = useState({
    days: null,
    hours: null,
    minutes: null,
    seconds: null,
  });

  const refreshTimer = (date) => {
 
  }

  return (
    <div>

    </div>
  )
}
